# CRUD with Spring Boot + Kotlin

This is a basic Spring Boot CRUD project using Kotlin. It is configured for Java version 21 
and includes dependencies for web and database development.


## Requirements

- JDK 21 or higher.
- Gradle 8.0 or higher (optional, Gradle Wrapper is included).
- PostgreSQL (if you want to run the application with a PostgreSQL database).